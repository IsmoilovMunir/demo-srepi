{
  "routes": [
    {
      "method": "GET",
      "path": "/photoarchives",
      "handler": "photoarchives.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/photoarchives/count",
      "handler": "photoarchives.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/photoarchives/:id",
      "handler": "photoarchives.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/photoarchives",
      "handler": "photoarchives.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/photoarchives/:id",
      "handler": "photoarchives.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/photoarchives/:id",
      "handler": "photoarchives.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
